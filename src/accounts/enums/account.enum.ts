export enum AccountStatus {
    Open = 'open',
    Active = 'active',
    Blocked = 'blocked',
    Deleted = 'deleted',
}

export enum AccountType {
    Checking = 'checking',
    Savings = 'savings',
}